# Chapter 1 enemy targeting and original fire damage

Branch: `coop`. This is a narrow Ch1 combat step, not campaign AI completion.

The original `orangesnail` and `firecrabSmall` source recipes now resolve a
living, ready co-op owner on the authority server. Selection checks range and
line of sight, keeps a valid spell attacker when appropriate, and rejects
players during cutscene capture, recovery hold, death or pause. It does not
replace the campaign's story leader or mutate the map's legacy Harry alias.
The crab still runs its original patrol/attack/throwing states; its shot keeps
the original `spellFireSmall` collision and damage path. Observed landing
callbacks during capture are deferred only within the same state generation.

Build `20260920-211431-100` passed UCC with 0 errors / 268 warnings.
HGame SHA256: `9d23231a9f2aea1f575fecbba3b60dc51023ae054a57a926432e5d574ad80603`.
M212Share SHA256: `fee12dd61e7ca7394281a1ad71f713f926f6b414d1dad1c24a689eb40257a48e`.
Recipe checks: 18 passed and one Windows symlink-privilege skip. `git diff
--check` passed.

The opt-in passive `AIInspect` run first observed 31 original placed enemies
(20 small fire crabs and 11 orange snails) after the original intro completed.
None naturally acquired the stationary owners in that snapshot because the
nearest observed crab was outside its attack range. The introspection run
therefore did not prove normal-map navigation or acquisition.

The separate explicit `AICombat` fixture spawned two original small crabs in
the clear intro corridor after both owners resumed. It did not call their AI
or attack functions directly. In host
`coop-host-20260920-211518-287-db40fa`, with actual network clients
`coop-join-20260920-211518-603-55ed80` and
`coop-join-20260920-211527-605-2d4a15`, both crabs entered original
`AttackHarry` and `throwing`, each targeting the corresponding registered
owner. The original projectile callback recorded independent real contacts:
`spellFireSmall1` from `firecrabSmall12` hit owner 1, and `spellFireSmall0`
from `firecrabSmall11` hit owner 0. Each health value changed 100→94.
The fixture reported `PASS_OWN_HITS`, then destroyed only its two spawned
crabs. Both clients had zero `Critical`/`Accessed None`; the server had zero
`Critical` and two existing map Harry `PreBeginPlay` `Accessed None` warnings.
All three owned processes were stopped and logs collected. The opt-in
`spellFireSmall` witness records health after the ordinary damage call and
does not grant damage itself.

The crab evidence establishes a local two-client original target/attack/hit
path. It does not establish natural placed-enemy traversal, death and target
switch under load, other enemy families, rendered animation or input, packet
impairment, or two physical PCs.

## Follow-up: original orange-snail body contact

Build `20260920-215852-841` passed UCC 0 errors / 268 warnings. HGame SHA256
`7d1908ee91bea108e521a0ff0ce1a3586c7bbacc92274a0b257814f8fa00c950`;
M212Share SHA256
`c40b0c1fb162c612778c418385a50af6abc2ea4db9d4330b584411250931571f`.
Explicit `AISnail` fixture host
`coop-host-20260920-215935-502-1a060e` and clients
`coop-join-20260920-215935-838-26a32d`,
`coop-join-20260920-215944-863-d948c7` completed the original intro.

Two original `orangesnail` instances were spawned 139.58 units from their
respective owners in a clear corridor. A dynamically spawned snail has no
authored PatrolPoint and falls into generic `stateIdle`; the fixture therefore
set its original no-route `PATROLTYPE_PATH_SEARCH` patrol mode. It did not call
the target selector, ram, collision or damage functions. Both original patrol
ticks selected different registered owners, entered `RamHarry`, moved into
them and entered `BumpedHarryPart1`. The post-`Super.TakeDamage` witness saw
the matching snail as attacker and `DamageType='Snail'` for each owner, with
health 100→70 on both. The fixture reported `PASS_OWN_BODY_HITS`, stopped its
own two snails and trails, and all three processes/logs were collected.
Both clients had zero Critical/Accessed None; the server had zero Critical and
the two known map-Harry `PreBeginPlay` Accessed None warnings.
The final source removes an unused retry that would have called `GotoState`
if the dynamic snail fell back to `stateIdle`. Final build
`20260920-220245-933` passed UCC 0/268; HGame
`00734373122947c46d607b98810170e2c5f454798f7b2f86e12d743ee9a201b4`,
M212Share `3cf10adcf9ad998eedbb22e82b7d80f5259b473c06455010790330cd125ef2ca`.
That removed branch was not used in the successful runtime run; the final
binary has not separately repeated the two-client fixture.

Two earlier fixture runs stopped at `BLOCKED`: the default dynamic
PatrolPoint mode had no authored route, and merely re-entering `patrol` sent
it back to `stateIdle`. Those runs were not counted as combat passes. The
successful result establishes the original snail ram/contact damage path in
this configured corridor. It does not validate placed-map patrol routes,
acid-trail touch damage, wider collision geometry, or two physical PCs. The
normal campaign remains blocked on these and broader story-state/travel
issues.
